{
  "name": "celron-pmr",
  "version": "1.0.0",
  "description": "CelRon Preventive Maintenance App",
  "scripts": {
    "start": "serve -s public",
    "dev": "flutter run -d chrome",
    "vercel-build": "rm -rf flutter && git clone https://github.com/flutter/flutter.git -b stable --depth 1 && ./flutter/bin/flutter build web --release --dart-define=GROQ_API_KEY=\"$GROQ_API_KEY\" --dart-define=GEMINI_API_KEY=\"$GEMINI_API_KEY\"",
    "build": "flutter build web --release"
  },
  "dependencies": {
    "firebase": "^12.12.1",
    "serve": "^14.2.1"
  }
}
